/**
 * faces/native/TrendChartFace.qml
 * The C++ painter for ShTrendChart, from the Shadcn.Native module the native
 * hmi-gui loader registers. Same `spec` contract as faces/canvas/TrendChartFace.qml;
 * Theme.face("TrendChart") picks this file only when the module is present.
 */
import QtQuick 2.15
import Shadcn.Native 1.0

TrendChartFace {
}
