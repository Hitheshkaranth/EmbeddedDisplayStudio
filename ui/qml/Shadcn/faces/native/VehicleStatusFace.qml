/**
 * faces/native/VehicleStatusFace.qml
 * The C++ painter for ShVehicleStatus, from the Shadcn.Native module the native
 * hmi-gui loader registers. Same `spec` contract as faces/canvas/VehicleStatusFace.qml;
 * Theme.face("VehicleStatus") picks this file only when the module is present.
 */
import QtQuick 2.15
import Shadcn.Native 1.0

VehicleStatusFace {
}
