/**
 * faces/native/AttitudeFace.qml
 * The C++ painter for ShAttitude, from the Shadcn.Native module the native
 * hmi-gui loader registers. Same `spec` contract as faces/canvas/AttitudeFace.qml;
 * Theme.face("Attitude") picks this file only when the module is present.
 */
import QtQuick 2.15
import Shadcn.Native 1.0

AttitudeFace {
}
