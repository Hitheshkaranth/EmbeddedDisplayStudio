/**
 * faces/native/AutoLevelFace.qml
 * The C++ painter for ShAutoLevel, from the Shadcn.Native module the native
 * hmi-gui loader registers. Same `spec` contract as faces/canvas/AutoLevelFace.qml;
 * Theme.face("AutoLevel") picks this file only when the module is present.
 */
import QtQuick 2.15
import Shadcn.Native 1.0

AutoLevelFace {
}
