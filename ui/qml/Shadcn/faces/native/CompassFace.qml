/**
 * faces/native/CompassFace.qml
 * The C++ painter for ShCompass, from the Shadcn.Native module the native
 * hmi-gui loader registers. Same `spec` contract as faces/canvas/CompassFace.qml;
 * Theme.face("Compass") picks this file only when the module is present.
 */
import QtQuick 2.15
import Shadcn.Native 1.0

CompassFace {
}
