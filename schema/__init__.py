"""
schema/ -- shared, normative implementations of the CONTRACT wire and file
formats, imported by host tooling and installed onto the target.

Currently: manifest.py, the single implementation of CONTRACT section 4.
"""
